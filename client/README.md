# Volunteer Match Board Client

React/Vite frontend for the Volunteer Match Board Milestone 3 final map update.

```powershell
npm install
npm run dev
```

The default map uses Leaflet/OpenStreetMap and requires no key. To use Google Maps, copy `.env.example` to `.env`, add `VITE_GOOGLE_MAPS_API_KEY`, and restart Vite.
